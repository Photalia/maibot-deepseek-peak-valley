# Changelog

## 1.1.0 - 2026-08-23

### 主要功能
- 价目表动态化：启动与定时轮询时从官方定价页解析并缓存，不再写死价格。
- 官方变更自动播报：检测到模型版本号或价格变化时，交由 Maisaka（麦麦）总结后播报。
- 周末全天低谷价：自 2026-08-23 00:00 起，周六、周日全天按低谷价计费，播报自动适配。
- `/checkdsapiupdate`：手动触发一次官方更新检查，由麦麦判断是否值得播报并回复结果。
- `/查询ds价格`：手动查询当前峰谷价格（与 `/峰谷` 等价的第二个入口）。

### 细节
- `/峰谷`、`/查询ds价格`、定时播报的价格展示格式恢复 v1.0.0 原样（`输入3输出9缓存0.1`），
  价格数值仍来自动态解析缓存，整数价不显示 `.0`。
- 新增 `[fetch]` 配置：轮询间隔、抓取超时、连续失败告警阈值。
- `[rate_limit]` 新增 `manual_check_cooldown_seconds`（默认 60s）防刷 `checkdsapiupdate`。
- 抓取失败回退上次成功缓存，连续失败多次才推送一次告警，避免刷屏。
- `/checkdsapiupdate` 抓取失败时明确回复失败原因，绝不静默。
- `/峰谷`、峰谷切换播报、按群去重与失败重试等原有行为保持不变。

## 1.0.0 - 2026-08-18

- 首次发布。
- 支持 DeepSeek V4 Flash／Pro 峰谷价格展示。
- 支持 `/峰谷` 即时查询。
- 支持 09:00、12:00、14:00、18:00 主动播报。
- 支持按用户及聊天流双层冷却。
- 支持按群、按切换点持久化去重与失败重试。
